{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://cut.dev/schemas/cut-footage-extract-v1.schema.json",
  "type": "object", "additionalProperties": false,
  "required": ["format", "version", "searchSha256", "indexSha256", "matchId", "label", "sourceSelection", "requestedHandles", "effectiveHandles", "finalRange", "toolchain", "output", "extractSha256"],
  "properties": { "format": { "const": "cut-footage-extract" }, "version": { "const": 1 }, "searchSha256": { "$ref": "#/$defs/sha256" }, "indexSha256": { "$ref": "#/$defs/sha256" }, "matchId": { "$ref": "#/$defs/id" }, "label": { "const": "candidate-only-not-cut-lock" }, "sourceSelection": { "$ref": "#/$defs/selection" }, "requestedHandles": { "$ref": "#/$defs/handles" }, "effectiveHandles": { "$ref": "#/$defs/handles" }, "finalRange": { "$ref": "#/$defs/range" }, "toolchain": { "$ref": "#/$defs/toolchain" }, "output": { "$ref": "#/$defs/output" }, "extractSha256": { "$ref": "#/$defs/sha256" } },
  "$defs": {
    "sha256": { "type": "string", "pattern": "^[a-f0-9]{64}$" }, "id": { "type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{0,127}$" },
    "locator": { "type": "string", "minLength": 1, "maxLength": 1024, "pattern": "^(?!/)(?!.*\\\\)(?!.*(?:^|/)\\.{1,2}(?:/|$))[^/]+(?:/[^/]+)*$" },
    "rational": { "type": "object", "additionalProperties": false, "required": ["numerator", "denominator"], "properties": { "numerator": { "type": "string", "pattern": "^(?:0|-?[1-9][0-9]*)$", "maxLength": 256 }, "denominator": { "type": "string", "pattern": "^[1-9][0-9]*$", "maxLength": 256 } }, "x-cut-semantic": ["cut-rational-reduced-v1"] },
    "range": { "type": "object", "additionalProperties": false, "required": ["semantics", "start", "end"], "properties": { "semantics": { "const": "half-open" }, "start": { "$ref": "#/$defs/rational" }, "end": { "$ref": "#/$defs/rational" } }, "x-cut-semantic": ["cut-half-open-range-v1"] },
    "selection": { "type": "object", "additionalProperties": false, "required": ["locator", "sha256", "streamIndex", "range"], "properties": { "locator": { "$ref": "#/$defs/locator" }, "sha256": { "$ref": "#/$defs/sha256" }, "streamIndex": { "type": "integer", "minimum": 0, "maximum": 1024 }, "range": { "$ref": "#/$defs/range" } } },
    "handles": { "type": "object", "additionalProperties": false, "required": ["head", "tail"], "properties": { "head": { "$ref": "#/$defs/rational" }, "tail": { "$ref": "#/$defs/rational" } }, "x-cut-semantic": ["cut-handles-non-negative-v1"] },
    "tool": { "type": "object", "additionalProperties": false, "required": ["name", "version"], "properties": { "name": { "type": "string", "minLength": 1, "maxLength": 128 }, "version": { "type": "string", "minLength": 1, "maxLength": 128 } } },
    "toolchain": { "type": "object", "additionalProperties": false, "required": ["ffmpeg", "ffprobe"], "properties": { "ffmpeg": { "allOf": [{ "$ref": "#/$defs/tool" }, { "properties": { "name": { "const": "ffmpeg" } } }] }, "ffprobe": { "allOf": [{ "$ref": "#/$defs/tool" }, { "properties": { "name": { "const": "ffprobe" } } }] } } },
    "outputStream": { "type": "object", "additionalProperties": false, "required": ["index", "type", "codec"], "properties": { "index": { "type": "integer", "minimum": 0, "maximum": 1024 }, "type": { "enum": ["video", "audio"] }, "codec": { "type": "string", "minLength": 1, "maxLength": 128 } } },
    "output": { "type": "object", "additionalProperties": false, "required": ["locator", "bytes", "sha256", "streams"], "properties": { "locator": { "$ref": "#/$defs/locator" }, "bytes": { "type": "integer", "minimum": 1, "maximum": 9007199254740991 }, "sha256": { "$ref": "#/$defs/sha256" }, "streams": { "type": "array", "minItems": 1, "maxItems": 64, "items": { "$ref": "#/$defs/outputStream" } } } }
  }
}
