{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:cut:schema:audio-audition-bindings:2",
  "title": "CUT audio audition bindings v2",
  "description": "Closed hash-bound mapping from catalog audio ids to exact project-local audio, rights evidence, and materialized semantic-analysis authority.",
  "type": "object",
  "additionalProperties": false,
  "required": ["bindingsSha256", "entries", "format", "version"],
  "properties": {
    "format": { "const": "cut-audio-audition-bindings" },
    "version": { "const": 2 },
    "entries": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1000,
      "items": { "$ref": "#/$defs/binding" }
    },
    "bindingsSha256": { "$ref": "#/$defs/sha256" }
  },
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "stableId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "pattern": "^[a-z0-9][a-z0-9._-]{0,127}$"
    },
    "projectLocator": {
      "type": "string",
      "minLength": 1,
      "maxLength": 4096,
      "pattern": "^(?!/)(?!.*(?:^|/)\\.\\.?(?:/|$))(?!.*//)(?!.*\\\\)(?!.*\\u0000).+$"
    },
    "semanticAnalysis": {
      "type": "object",
      "additionalProperties": false,
      "required": ["analysisSha256", "bytes", "fileSha256", "locator"],
      "properties": {
        "locator": { "$ref": "#/$defs/projectLocator" },
        "bytes": { "type": "integer", "minimum": 1, "maximum": 1048576 },
        "fileSha256": { "$ref": "#/$defs/sha256" },
        "analysisSha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "binding": {
      "type": "object",
      "additionalProperties": false,
      "required": ["audioLocator", "id", "rightsEvidenceLocator", "semanticAnalysis"],
      "properties": {
        "id": { "$ref": "#/$defs/stableId" },
        "audioLocator": { "$ref": "#/$defs/projectLocator" },
        "rightsEvidenceLocator": { "$ref": "#/$defs/projectLocator" },
        "semanticAnalysis": { "$ref": "#/$defs/semanticAnalysis" }
      }
    }
  },
  "x-cut-semanticConstraints": [
    "bindingsSha256 equals SHA-256 of canonical JSON for format, version and entries",
    "entry ids, audio locators, and semantic-analysis locators are unique",
    "every id matches one kind=audio catalog entry",
    "audio and rights-evidence locators resolve to authenticated project-local regular files",
    "semantic-analysis bytes and both hashes match the exact project-local file",
    "semantic source locator, bytes, hash, sample extent, sample rate and channels equal the bound audio candidate",
    "semantic canonical, normalization and taxonomy hashes plus current policy cross-links are revalidated",
    "provider inference is not re-executed from the materialized artifact"
  ]
}
